import { SortAlphabeticPipe } from './sort-alphabetic.pipe';

describe('SortAlphabeticPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAlphabeticPipe();
    expect(pipe).toBeTruthy();
  });
});
